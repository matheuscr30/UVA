#include <bits/stdc++.h>

using namespace std;
int n, m, grafo[55][55];

main()
{
  while(cin >> n >> m && (n+m))
  {
    for (int i = 0; i < n; i++)
      for (int j = 0; j < m ;j++)
        cin >> matriz[i][j];

    
  }
}
