#include <bits/stdc++.h>
#define endl '\n'
using namespace std;
typedef long long int ll;
ll n, left[20005], right[20005], d;

main()
{
	ll a, b;
	while(cin >> n && n)
	{
		for(ll i = 1; i <= n; i++)
		{
			cin >> left[i] >> right[i];
		}
	}
}
